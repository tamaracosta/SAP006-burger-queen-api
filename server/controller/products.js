const controller = {};

controller.getProduct = (req, res) => {
  res.send([]);
};

controller.getProductId = (req, res) => {
  res.send({});
};

controller.createProduct = (req, res) => {
  res.send({});
};

controller.updateProduct = (req, res) => {
  res.send({});
};

controller.deleteProduct = (req, res) => {
  res.send({});
};

module.exports = controller;
